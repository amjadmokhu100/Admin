@using PaperHelp.Models
@model IEnumerable<Service>
@{
    ViewBag.Title = "Index";
}
<br />
<br />
<br />
<br />
<br />
<br />

<section class="portfolio" id="portfolio">

    <h1 class="heading"> <span>Services</span> </h1>
    
    <div class="box-container">
        @foreach (var s in Model){

            if (s.Sale == false){

        <div class="box">
            <img src="@Url.Content(s.Photo)" alt="">
            <div class="content">
                <h3>@s.Name</h3>
                <p>@s.Description</p>
                <a href="@Url.Action("Order", "Services", new { id = s.Id })" class="btn" style="text-align:center; width:150px">Order Now</a>
            </div>
        </div>
                }

                }
            
        </div>
</section>


<section class="portfolio" id="portfolio">

    <h1 class="heading"> Offers <span>Services</span> </h1>

    <div class="box-container">
    @foreach (var ss in Model)
    {

        if (ss.Sale==true)
        {

        
        <div class="box">
            <span>Sale</span>
            <img src="@Url.Content(ss.Photo)" alt="">
            <div class="content">
                <h3>@ss.Name</h3>
                <p>@ss.Description</p>
                <a href="@Url.Action("Order", "Services", new { id = ss.Id })" class="btn" style="text-align:center; width:150px">Order Now</a>
            </div>
        </div>
        }
    }

    </div> 
</section>